## Appendix B: Supplementary analyses (monolinguals)

```{r}
#| label: tab-mono
#| results: asis
mono_table %>%  
  mutate(
    Parameter = str_replace(Parameter, "x", "&#215;"), 
    Metric = case_when(
    identifier == "VOT_Intercept" ~ "VOT", 
    identifier == "COG_Intercept" ~ "COG", 
    identifier == "KT_Intercept" ~ "KT", 
    identifier == "RI_Intercept" ~ "RI", 
    identifier == "SD_Intercept" ~ "SD", 
    identifier == "SK_Intercept" ~ "SK", 
    TRUE ~ " ")) %>% 
  select(-identifier) %>% 
  knitr::kable(format = "markdown", 
    align = c("l", "l", "r", "r", "r", "r"), 
    caption = "Model summary for VOT and burst metrics as a function of language (English, Spanish), phoneme (/d/, /t/), F1, F2, and item repetition for monolingual coronal stops. The percentage of the HDI contained within the ROPE is based on an effect size of &plusmn;0.1. {#tbl-mono}")
```



```{r}
#| label: fig-monolinguals-d-t
#| fig-cap: "Posterior distribution comparing the short-lag stops, English /d/ and Spanish /t/. The white point represents the posterior mean &pm;95% HDI and the grey region represents the ROPE (&pm;0.1)."
knitr::include_graphics(
  here("figs", "mono_post_hoc_dt.pdf")
)
```
